Description:
    Adds jscs, rubocop, and scss-lint config files to your rails app

Example:
    rails generate lint_configs

    This will create:
        .jscsrc
        .rubocop.yml
        .scss-lint.yml
